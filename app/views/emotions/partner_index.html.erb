<div class='w-7/8 mx-auto max-w-screen-xl'>
  <div class="container mx-auto p-4">
  <h1 class="text-center text-3xl font-bold py-6">PartnerEmotion記録</h1>
    <div class="p-6 shadow-lg rounded-lg bg-white max-w-2xl mx-auto">
      <% if @partner %>
        <% if @partner_emotions.exists? %>
        <%= render partial: 'emotion', collection: @partner_emotions, as: :emotion %>
          <%= paginate @partner_emotions %>
        <% else %>
          <p class="text-center">Partnerはまだ投稿していないようだね。</p>
        <% end %>
      <% else %>
        <p class="text-center">Partnerがいないようだね。Partnerを招待しよう!</p>
      <% end %>
    </div>
  </div>
</div>
